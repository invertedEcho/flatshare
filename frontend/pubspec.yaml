name: flatshare
description: "Flatshare: A free, open-source mobile app to manage tasks, shopping lists, and more for shared living spaces—no ads, no privacy invasions."

publish_to: 'none'

version: 2.0.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: 1.0.6
  http: 1.2.1
  flutter_secure_storage: 9.2.2
  provider: 6.1.2
  flutter_dotenv: 5.1.0
  collection: 1.18.0
  animated_custom_dropdown: 3.1.1
  share_plus: 9.0.0
  go_router: 14.2.1
  socket_io_client: 2.0.3+1
  flutter_glow: 0.3.2
  firebase_core: 3.4.0
  firebase_messaging: 15.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: 3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
